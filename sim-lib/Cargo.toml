[package]
name = "sim-lib"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
rand = "0.10.1"
wasm-bindgen = "0.2"
getrandom = { version = "0.4", features = ["wasm_js"] }
